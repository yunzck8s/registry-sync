apiVersion: v1
kind: Service
metadata:
  name: registry-sync
  namespace: registry-sync
  labels:
    app: registry-sync
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: registry-sync
